# Environment Variables

# Required: Gemini API Key for server-side operations
GEMINI_API_KEY=

# Optional: Allowed origin for CORS (default: *)
ALLOWED_ORIGIN=

# Optional: Application environment (default: production)
APP_ENV=development

# Optional: Rate limit per minute per IP (default: 30)
RATE_LIMIT_PER_MIN=30
